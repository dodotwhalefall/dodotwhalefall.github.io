---
title:      二叉树
subtitle:   整张学习笔记
date:       2020-8-24
author:     jam
header-img: img/post-bg-re-vs-ng2.jpg
catalog: true
tags:
    - 二叉树
---

### 1. 非递归中序遍历
```cpp
void Inorder(Bitree T){
   InitStack(s);        //初始化栈以及所需变量
   Bitree p=T;
   while(p||!IsEmpty(s)){//循环条件
       if(p){            //执行单元的条件
           Push(S,p);    //左全入栈
           p=p->lchild
       }
       else{
           Pop(S,p);visit(p);//左先访问然后根
           p=p->rchild;        //左和根的右都要访问写在一起
       }
   }
}
```
### 2. 非递归后序遍历
```cpp
    void Postorder(Bitree T){
        InitStack()

    }
```